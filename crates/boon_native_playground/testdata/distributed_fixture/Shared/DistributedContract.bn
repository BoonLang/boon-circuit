FUNCTION client_label() {
    TEXT { Distributed program fixture }
}

FUNCTION response(count) {
    BLOCK {
        response_count: count
        [
            status: TEXT { accepted }
            count: response_count
        ]
    }
}
