# Retired: Cells Legacy Playground Reality Parity

This plan is retired. Cells performance and correctness must be verified through
the generic runtime/list/currentness architecture and native GPU evidence, not
through the deleted Ply playground.

Cells-specific work must not add runtime/compiler/renderer hacks. Fix generic
PlanExecutor/runtime dependencies, retained rendering, input scheduling, and
proof separation instead.
