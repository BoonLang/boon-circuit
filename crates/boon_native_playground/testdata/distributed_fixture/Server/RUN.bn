store: [
    request_received: Session/store.session_changed

    server_count:
        0 |> HOLD server_count {
            LATEST {
                store.request_received |> THEN { server_count + 1 }
            }
        }
]

outputs: [
    api_response: DistributedContract/response(count: store.server_count)
]
